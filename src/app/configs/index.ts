const punkApiRootEndpoint = import.meta.env.VITE_PUNK_API_ROOT_ENDPOINT
console.log(punkApiRootEndpoint)
// Здесь можно реализовать конвертацию и/или валидацию значений из .env или других источников
export const configs = {
  punkApiRootEndpoint
}
